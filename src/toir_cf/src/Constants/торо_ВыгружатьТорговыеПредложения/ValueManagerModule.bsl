#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	Если Значение И НЕ ТорговыеПредложенияСлужебный.ИспользоватьФункционалПубликации() Тогда 
		Значение = Ложь;
		ОбщегоНазначения.СообщитьПользователю(
			НСтр("ru = 'Значение константы не может быть установлено в Истину, т.к. в системе не настроена функциональность публикации торговых предложений.'"));
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
