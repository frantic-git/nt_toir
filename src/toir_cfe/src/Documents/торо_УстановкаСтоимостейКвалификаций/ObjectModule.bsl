
#Область ОбработчикиСобытий

//++ Проф-ИТ, #156, Корнилов М.С., 27.12.2023
&ИзменениеИКонтроль("ПередЗаписью")
Процедура проф_ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	#Удаление
	Если ЭтоНовый() Тогда
		Автор = Пользователи.ТекущийПользователь();
	КонецЕсли;
	#КонецУдаления
	#Вставка
	Если ЭтоНовый()
		И ОбменДанными.Загрузка = Ложь Тогда
		Автор = Пользователи.ТекущийПользователь();
	КонецЕсли;
	#КонецВставки
КонецПроцедуры
//-- Проф-ИТ, #156, Корнилов М.С., 27.12.2023

#КонецОбласти

